# Instructions for 04-custom-look-and-feel-slots

1. Copy the `after-files/src/app/page.tsx` into `01-build-with-agents/my-copilot-app/src/app/page.tsx`.
2. Run `npm run dev` in `01-build-with-agents/my-copilot-app`.
3. Verify that the UI uses custom slots (custom message view and styled input) as defined in the docs.
4. Ensure you can interact with the agent properly.
5. Fill out `report.md`.
