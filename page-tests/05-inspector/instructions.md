# Instructions for 05-inspector

1. Copy the `after-files/src/app/layout.tsx` into `01-build-with-agents/my-copilot-app/src/app/layout.tsx`.
2. Run `npm run dev` in `01-build-with-agents/my-copilot-app`.
3. Check the page in your browser and verify that the Inspector overlay is NO longer visible.
4. Fill out `report.md`.
