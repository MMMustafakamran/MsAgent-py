[project]
name = "my-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Mustafa Kamran", email = "mmmustafakamran@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "agent-framework-ag-ui>=1.0.1",
    "agent-framework-openai>=1.12.0",
    "python-dotenv>=1.2.2",
    "uvicorn>=0.52.1",
]

[project.scripts]
my-agent = "my_agent:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
